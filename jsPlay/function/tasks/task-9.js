console.log(subtract(10, 5));

function subtract(a, b) {
  return a - b;
}

console.log(divide(10, 2));

const divide = (a, b) => a / b;

// It will throw a ReferenceError because it was declared using const